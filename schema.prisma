// ----- ENUMS (put near the top of prisma/schema.prisma) -----
enum UserRole {
  GUEST
  HOST
  ADMIN
}

enum KycStatus {
  PENDING
  APPROVED
  REJECTED
}

enum DocType {
  V5C
  INSURANCE
  MOT
  ROAD_TAX
  DRIVER_LICENSE
  OTHER
}

enum DocStatus {
  UPLOADED
  VERIFIED
  REJECTED
  EXPIRED
}

enum VehicleStatus {
  DRAFT
  ACTIVE
  SUSPENDED
}

enum BookingStatus {
  QUOTED
  PENDING
  CONFIRMED
  CANCELED
  COMPLETED
  DISPUTED
}

enum PayoutStatus {
  CREATED
  PAID
  FAILED
  CANCELED
}

}
