// ----- ENUMS (put near the top of prisma/schema.prisma) -----
enum UserRole {
  GUEST
  HOST
  ADMIN
}

enum KycStatus {
  PENDING
  APPROVED
  REJECTED
}

enum DocType {
  V5C
  INSURANCE
  MOT
  ROAD_TAX
  DRIVER_LICENSE
  OTHER
}

enum DocStatus {
  UPLOADED
  VERIFIED
  REJECTED
  EXPIRED
}

enum VehicleStatus {
  DRAFT
  ACTIVE
  SUSPENDED
}

enum BookingStatus {
  QUOTED
  PENDING
  CONFIRMED
  CANCELED
  COMPLETED
  DISPUTED
}

enum PayoutStatus {
  CREATED
  PAID
  FAILED
  CANCELED
}
Then:
Format locally to double-check

bash
Copy code
npx prisma format
Commit & push

bash
Copy code
git add prisma/schema.prisma
git commit -m "Fix Prisma enum syntax (multiline values)"
git push
Trigger a new Netlify deploy (it will run prisma generate during postinstall and should pass).

Extra sanity checks (optional but helpful)
Ensure your schema header is correct:

prisma
Copy code
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
Make sure the file path is prisma/schema.prisma (default Prisma location).

Add a format script so Netlify CI fails early if formatting is off:

json
Copy code
// package.json
"scripts": {
  "prisma:format": "prisma format",
  "postinstall": "prisma generate"
}
